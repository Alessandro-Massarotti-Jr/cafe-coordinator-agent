services:
  backend:
    build:
      context: ./backend
      dockerfile: ./.docker/Dockerfile.dev
    command: "/home/node/app/.docker/start-infra.sh"
    container_name: backend
    environment:
      - PORT=3000
      - OLLAMA_URL=http://ollama:11434/api
    volumes:
      - ./backend:/home/node/app:cached
    expose:
      - "3000"
    depends_on:
      - ollama
      - products-mcp

  products-mcp:
    build:
      context: ./products-mcp
      dockerfile: ./.docker/Dockerfile.dev
    container_name: products-mcp
    environment:
      - PORT=3001
    volumes:
      - ./products-mcp:/home/node/app:cached
    expose:
      - "3001"

  frontend:
    build:
      context: ./frontend
      dockerfile: ./.docker/Dockerfile
    container_name: frontend
    ports:
      - "8080:80"
    volumes:
      - ./frontend/index.html:/usr/share/nginx/html/index.html
      - ./frontend/index.css:/usr/share/nginx/html/index.css
      - ./frontend/index.js:/usr/share/nginx/html/index.js
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend

  ollama:
    image: ollama/ollama:0.19.0-rc0
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    entrypoint: >
      /bin/sh -c "
      ollama serve &
      sleep 5 &&
      ollama pull gemma4 &&
      ollama pull embeddinggemma &&
      wait
      "
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  ollama_data:
  qdrant_data:
